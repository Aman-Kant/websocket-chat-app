# Application name
spring.application.name=app

# Server port (Render will typically set this automatically)
server.port=${PORT:8080}

# Logging configuration (optional but useful for monitoring)
logging.level.org.springframework.web=INFO
logging.level.com.chat.app=DEBUG

# WebSocket properties (optional for fine-tuning)
spring.websocket.message-size=65536
